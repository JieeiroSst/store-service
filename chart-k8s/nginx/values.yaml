replicaCount: 1
image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent
persistence:
  enabled: true
  size: 1Gi
ingress:
  enabled: true
  hosts:
    - host: my-nginx.local
      paths:
        - path: /nginx
          pathType: Prefix