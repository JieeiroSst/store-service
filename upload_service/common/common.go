package common

var (
	Success = 200
)

var (
	Database    = "upload"
	TableUpload = "uploads"
)

var (
	Production = "production"
)

var (
	GetImageByIdKeyCache = "us:get_image_by_id_%v"
	GetImagesKeyCache    = "us:get_images"
)
