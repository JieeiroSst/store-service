{{- if .Values.recompenseService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.recompenseService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.recompenseService.name }}-deployment
  minReplicas: {{ .Values.recompenseService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.recompenseService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.recompenseService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
