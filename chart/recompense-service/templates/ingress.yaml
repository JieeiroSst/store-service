{{- if .Values.recompenseService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.recompenseService.name }}-ingress
  {{- with .Values.recompenseService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.recompenseService.ingress.className }}
  rules:
  - host: {{ .Values.recompenseService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.recompenseService.ingress.path }}
        pathType: {{ .Values.recompenseService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.recompenseService.service.name }}
            port:
              number: 80
{{- end }}
