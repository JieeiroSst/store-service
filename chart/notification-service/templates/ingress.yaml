{{- if .Values.notificationService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.notificationService.name }}-ingress
  {{- with .Values.notificationService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.notificationService.ingress.className }}
  rules:
  - host: {{ .Values.notificationService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.notificationService.ingress.path }}
        pathType: {{ .Values.notificationService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.notificationService.service.name }}
            port:
              number: 80
{{- end }}
