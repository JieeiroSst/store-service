{{- if .Values.arrange.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.arrange.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.arrange.name }}-deployment
  minReplicas: {{ .Values.arrange.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.arrange.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.arrange.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
