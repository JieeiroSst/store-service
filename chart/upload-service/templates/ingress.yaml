{{- if .Values.uploadService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.uploadService.name }}-ingress
  {{- with .Values.uploadService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.uploadService.ingress.className }}
  rules:
  - host: {{ .Values.uploadService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.uploadService.ingress.path }}
        pathType: {{ .Values.uploadService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.uploadService.service.name }}
            port:
              number: 80
{{- end }}
