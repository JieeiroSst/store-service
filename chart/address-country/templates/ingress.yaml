{{- if .Values.addressCountry.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.addressCountry.name }}-ingress
  {{- with .Values.addressCountry.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.addressCountry.ingress.className }}
  rules:
  - host: {{ .Values.addressCountry.ingress.host }}
    http:
      paths:
      - path: {{ .Values.addressCountry.ingress.path }}
        pathType: {{ .Values.addressCountry.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.addressCountry.service.name }}
            port:
              number: 80
{{- end }}
