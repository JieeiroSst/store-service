{{- if .Values.addressCountry.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.addressCountry.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.addressCountry.name }}-deployment
  minReplicas: {{ .Values.addressCountry.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.addressCountry.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.addressCountry.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
