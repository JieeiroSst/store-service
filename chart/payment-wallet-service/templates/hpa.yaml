{{- if .Values.paymentWalletService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.paymentWalletService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.paymentWalletService.name }}-deployment
  minReplicas: {{ .Values.paymentWalletService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.paymentWalletService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.paymentWalletService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
