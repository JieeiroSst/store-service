{{- if .Values.lottery.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.lottery.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.lottery.name }}-deployment
  minReplicas: {{ .Values.lottery.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.lottery.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.lottery.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
