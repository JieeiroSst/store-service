{{- if .Values.lottery.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.lottery.name }}-ingress
  {{- with .Values.lottery.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.lottery.ingress.className }}
  rules:
  - host: {{ .Values.lottery.ingress.host }}
    http:
      paths:
      - path: {{ .Values.lottery.ingress.path }}
        pathType: {{ .Values.lottery.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.lottery.service.name }}
            port:
              number: 80
{{- end }}
