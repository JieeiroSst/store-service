{{- if .Values.cataloguesService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.cataloguesService.name }}-ingress
  {{- with .Values.cataloguesService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.cataloguesService.ingress.className }}
  rules:
  - host: {{ .Values.cataloguesService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.cataloguesService.ingress.path }}
        pathType: {{ .Values.cataloguesService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.cataloguesService.service.name }}
            port:
              number: 80
{{- end }}
