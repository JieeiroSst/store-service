{{- if .Values.cataloguesService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.cataloguesService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.cataloguesService.name }}-deployment
  minReplicas: {{ .Values.cataloguesService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.cataloguesService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.cataloguesService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
