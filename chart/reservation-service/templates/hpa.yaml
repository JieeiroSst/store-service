{{- if .Values.reservationService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.reservationService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.reservationService.name }}-deployment
  minReplicas: {{ .Values.reservationService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.reservationService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.reservationService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
