{{- if .Values.parkingLotService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.parkingLotService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.parkingLotService.name }}-deployment
  minReplicas: {{ .Values.parkingLotService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.parkingLotService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.parkingLotService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
