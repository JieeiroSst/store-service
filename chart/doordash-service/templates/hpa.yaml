{{- if .Values.doordashService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.doordashService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.doordashService.name }}-deployment
  minReplicas: {{ .Values.doordashService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.doordashService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.doordashService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
