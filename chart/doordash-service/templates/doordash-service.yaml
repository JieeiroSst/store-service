apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.doordashService.name }}-deployment
spec:
{{- if not .Values.doordashService.autoscaling.enabled }}
  replicas: {{ .Values.doordashService.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.doordashService.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.doordashService.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.doordashService.name }}-deployment
        image: {{ .Values.doordashService.image.name }}:{{ .Values.doordashService.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.doordashService.image.port }}
        env:
          - name: PORT_HTTP_SERVER
            value: "{{ .Values.doordashService.image.port }}"
          - name: POSTGRES_HOST
            value: {{ .Values.doordashService.env.postgresHost | quote }}
          - name: POSTGRES_PORT
            value: {{ .Values.doordashService.env.postgresPort | quote }}
          - name: POSTGRES_USER
            value: {{ .Values.doordashService.env.postgresUser | quote }}
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.doordashService.name }}-secret
                key: postgresPassword
          - name: POSTGRES_DBNAME
            value: {{ .Values.doordashService.env.postgresDbname | quote }}
          - name: POSTGRES_SSLMODE
            value: {{ .Values.doordashService.env.postgresSSLMode | quote }}
          - name: USER_SERVICE_BASE_URL
            value: {{ .Values.doordashService.env.userServiceBaseURL | quote }}
          - name: RESTAURANT_SERVICE_BASE_URL
            value: {{ .Values.doordashService.env.restaurantServiceBaseURL | quote }}
          - name: PAYMENT_SERVICE_BASE_URL
            value: {{ .Values.doordashService.env.paymentServiceBaseURL | quote }}
          - name: NOTIFICATION_SERVICE_BASE_URL
            value: {{ .Values.doordashService.env.notificationServiceBaseURL | quote }}
        resources:
          {{- toYaml .Values.doordashService.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: doordash-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.doordashService.name }}-secret
type: Opaque
stringData:
  # Dev defaults from values.yaml - override with --set or a
  # values-prod.yaml before deploying anywhere real.
  postgresPassword: {{ .Values.doordashService.env.postgresPassword | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.doordashService.service.name }}
spec:
  type: {{ .Values.doordashService.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.doordashService.image.port }}
  selector:
    app: {{ .Values.doordashService.name }}-deployment
