{{- if .Values.admanagement.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.admanagement.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.admanagement.name }}-deployment
  minReplicas: {{ .Values.admanagement.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.admanagement.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.admanagement.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
