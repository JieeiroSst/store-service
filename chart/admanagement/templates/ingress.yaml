{{- if .Values.admanagement.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.admanagement.name }}-ingress
  {{- with .Values.admanagement.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.admanagement.ingress.className }}
  rules:
  - host: {{ .Values.admanagement.ingress.host }}
    http:
      paths:
      - path: {{ .Values.admanagement.ingress.path }}
        pathType: {{ .Values.admanagement.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.admanagement.service.name }}
            port:
              number: 80
{{- end }}
