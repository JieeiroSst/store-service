{{- if .Values.accountingService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.accountingService.name }}-ingress
  {{- with .Values.accountingService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.accountingService.ingress.className }}
  rules:
  - host: {{ .Values.accountingService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.accountingService.ingress.path }}
        pathType: {{ .Values.accountingService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.accountingService.service.name }}
            port:
              number: 80
{{- end }}
