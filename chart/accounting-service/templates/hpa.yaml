{{- if .Values.accountingService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.accountingService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.accountingService.name }}-deployment
  minReplicas: {{ .Values.accountingService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.accountingService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.accountingService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
