{{- if .Values.bonuslinkService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.bonuslinkService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.bonuslinkService.name }}-deployment
  minReplicas: {{ .Values.bonuslinkService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bonuslinkService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bonuslinkService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
