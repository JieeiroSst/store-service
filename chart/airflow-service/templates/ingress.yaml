{{- if .Values.airflow.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.airflow.name }}-ingress
  {{- with .Values.airflow.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.airflow.ingress.className }}
  rules:
  - host: {{ .Values.airflow.ingress.host }}
    http:
      paths:
      - path: {{ .Values.airflow.ingress.path }}
        pathType: {{ .Values.airflow.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.airflow.service.name }}
            port:
              number: 80
{{- end }}
