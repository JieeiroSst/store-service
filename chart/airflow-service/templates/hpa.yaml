{{- if .Values.airflow.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.airflow.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.airflow.name }}-deployment
  minReplicas: {{ .Values.airflow.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.airflow.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.airflow.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
