{{- if .Values.consumerService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.consumerService.name }}-ingress
  {{- with .Values.consumerService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.consumerService.ingress.className }}
  rules:
  - host: {{ .Values.consumerService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.consumerService.ingress.path }}
        pathType: {{ .Values.consumerService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.consumerService.service.name }}
            port:
              number: 80
{{- end }}
