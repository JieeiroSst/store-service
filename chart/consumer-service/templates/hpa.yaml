{{- if .Values.consumerService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.consumerService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.consumerService.name }}-deployment
  minReplicas: {{ .Values.consumerService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.consumerService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.consumerService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
