apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.consumerService.name }}-deployment
spec:
{{- if not .Values.consumerService.autoscaling.enabled }}
  replicas: {{ .Values.consumerService.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.consumerService.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.consumerService.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.consumerService.name }}-deployment
        image: {{ .Values.consumerService.image.name }}:{{ .Values.consumerService.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.consumerService.image.port }}
        resources:
          {{- toYaml .Values.consumerService.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: consumer-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.consumerService.service.name }}
spec:
  type: {{ .Values.consumerService.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.consumerService.image.port }}
  selector:
    app: {{ .Values.consumerService.name }}-deployment
