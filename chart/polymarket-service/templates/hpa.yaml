{{- if .Values.polymarketService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.polymarketService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.polymarketService.name }}-deployment
  minReplicas: {{ .Values.polymarketService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.polymarketService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.polymarketService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
