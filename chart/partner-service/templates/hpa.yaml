{{- if .Values.partnerService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.partnerService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.partnerService.name }}-deployment
  minReplicas: {{ .Values.partnerService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.partnerService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.partnerService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
