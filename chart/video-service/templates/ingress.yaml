{{- if .Values.videoService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.videoService.name }}-ingress
  {{- with .Values.videoService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.videoService.ingress.className }}
  rules:
  - host: {{ .Values.videoService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.videoService.ingress.path }}
        pathType: {{ .Values.videoService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.videoService.service.name }}
            port:
              number: 80
{{- end }}
