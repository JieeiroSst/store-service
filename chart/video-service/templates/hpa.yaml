{{- if .Values.videoService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.videoService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.videoService.name }}-deployment
  minReplicas: {{ .Values.videoService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.videoService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.videoService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
