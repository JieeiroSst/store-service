{{- if .Values.roomService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.roomService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.roomService.name }}-deployment
  minReplicas: {{ .Values.roomService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.roomService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.roomService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
