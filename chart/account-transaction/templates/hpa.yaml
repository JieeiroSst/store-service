{{- if .Values.accountTransaction.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.accountTransaction.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.accountTransaction.name }}-deployment
  minReplicas: {{ .Values.accountTransaction.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.accountTransaction.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.accountTransaction.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
