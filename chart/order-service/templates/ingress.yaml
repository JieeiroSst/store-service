{{- if .Values.orderService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.orderService.name }}-ingress
  {{- with .Values.orderService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.orderService.ingress.className }}
  rules:
  - host: {{ .Values.orderService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.orderService.ingress.path }}
        pathType: {{ .Values.orderService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.orderService.service.name }}
            port:
              number: 80
{{- end }}
