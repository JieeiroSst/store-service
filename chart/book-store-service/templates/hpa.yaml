{{- if .Values.bookStoreService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.bookStoreService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.bookStoreService.name }}-deployment
  minReplicas: {{ .Values.bookStoreService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bookStoreService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bookStoreService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
