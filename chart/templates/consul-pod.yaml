apiVersion: v1
kind: Pod
metadata:
  name: consul-service
spec:
  containers:
    - name: example
      image: 'consul:latest'
          env:
            - name: NODE_ENV
              value: production
  restartPolicy: Never

