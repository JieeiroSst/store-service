{{- if .Values.basketService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.basketService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.basketService.name }}-deployment
  minReplicas: {{ .Values.basketService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.basketService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.basketService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
