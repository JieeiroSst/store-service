apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.geoservice.name }}-deployment
spec:
{{- if not .Values.geoservice.autoscaling.enabled }}
  replicas: {{ .Values.geoservice.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.geoservice.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.geoservice.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.geoservice.name }}-deployment
        image: {{ .Values.geoservice.image.name }}:{{ .Values.geoservice.image.tag }}
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: {{ .Values.geoservice.image.port }}
        resources:
          {{- toYaml .Values.geoservice.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: geoservice.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.geoservice.service.name }}
spec:
  type: {{ .Values.geoservice.service.type }}
  ports:
  - name: http
    port: 80
    targetPort: {{ .Values.geoservice.image.port }}
  selector:
    app: {{ .Values.geoservice.name }}-deployment
