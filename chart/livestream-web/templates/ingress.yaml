{{- if .Values.livestreamWeb.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.livestreamWeb.name }}-ingress
  {{- with .Values.livestreamWeb.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.livestreamWeb.ingress.className }}
  rules:
  - host: {{ .Values.livestreamWeb.ingress.host }}
    http:
      paths:
      - path: {{ .Values.livestreamWeb.ingress.path }}
        pathType: {{ .Values.livestreamWeb.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.livestreamWeb.service.name }}
            port:
              number: 80
{{- end }}
