apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.roomWeb.name }}-deployment
spec:
{{- if not .Values.roomWeb.autoscaling.enabled }}
  replicas: {{ .Values.roomWeb.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.roomWeb.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.roomWeb.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.roomWeb.name }}-deployment
        image: {{ .Values.roomWeb.image.name }}:{{ .Values.roomWeb.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.roomWeb.image.port }}
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.roomWeb.image.port }}
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.roomWeb.image.port }}
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- toYaml .Values.roomWeb.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.roomWeb.service.name }}
spec:
  type: {{ .Values.roomWeb.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.roomWeb.image.port }}
  selector:
    app: {{ .Values.roomWeb.name }}-deployment
