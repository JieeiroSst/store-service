{{- if .Values.bookingMiniService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.bookingMiniService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.bookingMiniService.name }}-deployment
  minReplicas: {{ .Values.bookingMiniService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bookingMiniService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bookingMiniService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
