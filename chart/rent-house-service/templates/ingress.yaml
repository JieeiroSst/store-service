{{- if .Values.rentHouseService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.rentHouseService.name }}-ingress
  {{- with .Values.rentHouseService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.rentHouseService.ingress.className }}
  rules:
  - host: {{ .Values.rentHouseService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.rentHouseService.ingress.path }}
        pathType: {{ .Values.rentHouseService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.rentHouseService.service.name }}
            port:
              number: 80
{{- end }}
