{{- range .Values.postgres.serviceDatabases }}
{{- $secretName := printf "%s-db-credentials" .name }}
{{- $existing := lookup "v1" "Secret" $.Release.Namespace $secretName }}
{{- $password := "" }}
{{- if $existing }}
  {{- $password = index $existing.data "POSTGRES_PASSWORD" | b64dec }}
{{- else }}
  {{- $password = randAlphaNum 24 }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
type: Opaque
data:
  POSTGRES_USER: {{ .role | b64enc }}
  POSTGRES_PASSWORD: {{ $password | b64enc }}
---
{{- end }}
