apiVersion: v1
kind: Service
metadata:
  name: postgresdb-nodeport
  labels:
    app: postgresdb
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 31000
  selector:
    app: postgresdb
