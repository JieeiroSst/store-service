{{- if .Values.couponService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.couponService.name }}-ingress
  {{- with .Values.couponService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.couponService.ingress.className }}
  rules:
  - host: {{ .Values.couponService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.couponService.ingress.path }}
        pathType: {{ .Values.couponService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.couponService.service.name }}
            port:
              number: 80
{{- end }}
