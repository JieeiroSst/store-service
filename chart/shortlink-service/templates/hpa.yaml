{{- if .Values.shortlinkService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.shortlinkService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.shortlinkService.name }}-deployment
  minReplicas: {{ .Values.shortlinkService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.shortlinkService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.shortlinkService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
