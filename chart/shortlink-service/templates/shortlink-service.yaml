apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.shortlinkService.name }}-deployment
spec:
{{- if not .Values.shortlinkService.autoscaling.enabled }}
  replicas: {{ .Values.shortlinkService.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.shortlinkService.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.shortlinkService.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.shortlinkService.name }}-deployment
        image: {{ .Values.shortlinkService.image.name }}:{{ .Values.shortlinkService.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.shortlinkService.image.port }}
        env:
          - name: PORT
            value: "{{ .Values.shortlinkService.image.port }}"
        resources:
          {{- toYaml .Values.shortlinkService.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: shortlink-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.shortlinkService.service.name }}
spec:
  type: {{ .Values.shortlinkService.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.shortlinkService.image.port }}
  selector:
    app: {{ .Values.shortlinkService.name }}-deployment
