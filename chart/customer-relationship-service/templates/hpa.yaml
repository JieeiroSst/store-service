{{- if .Values.customerRelationshipService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.customerRelationshipService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.customerRelationshipService.name }}-deployment
  minReplicas: {{ .Values.customerRelationshipService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.customerRelationshipService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.customerRelationshipService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
