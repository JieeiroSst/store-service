{{- if .Values.movieRecommendationService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.movieRecommendationService.name }}-ingress
  {{- with .Values.movieRecommendationService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.movieRecommendationService.ingress.className }}
  rules:
  - host: {{ .Values.movieRecommendationService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.movieRecommendationService.ingress.path }}
        pathType: {{ .Values.movieRecommendationService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.movieRecommendationService.service.name }}
            port:
              number: 80
{{- end }}
