{{- if .Values.automaticPayment.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.automaticPayment.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.automaticPayment.name }}-deployment
  minReplicas: {{ .Values.automaticPayment.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.automaticPayment.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.automaticPayment.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
