apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.automaticPayment.name }}-deployment
spec:
{{- if not .Values.automaticPayment.autoscaling.enabled }}
  replicas: {{ .Values.automaticPayment.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.automaticPayment.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.automaticPayment.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.automaticPayment.name }}-deployment
        image: {{ .Values.automaticPayment.image.name }}:{{ .Values.automaticPayment.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.automaticPayment.image.port }}
        env:
          - name: HTTP_PORT
            value: "{{ .Values.automaticPayment.image.port }}"
        resources:
          {{- toYaml .Values.automaticPayment.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: automatic-payment-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.automaticPayment.service.name }}
spec:
  type: {{ .Values.automaticPayment.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.automaticPayment.image.port }}
  selector:
    app: {{ .Values.automaticPayment.name }}-deployment
