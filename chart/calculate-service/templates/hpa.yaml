{{- if .Values.calculate.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.calculate.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.calculate.name }}-deployment
  minReplicas: {{ .Values.calculate.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.calculate.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.calculate.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
