apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.calculate.name }}-deployment
spec:
  replicas: {{ .Values.calculate.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.calculate.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.calculate.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.calculate.name }}-deployment
        image: {{ .Values.calculate.image.name }}:{{ .Values.calculate.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.calculate.image.port }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: calculate-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.calculate.service.name }}
spec:
  type: {{ .Values.calculate.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.calculate.image.port }}
  selector:
    app: {{ .Values.calculate.name }}-deployment
