{{- if .Values.calculate.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.calculate.name }}-ingress
  {{- with .Values.calculate.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.calculate.ingress.className }}
  rules:
  - host: {{ .Values.calculate.ingress.host }}
    http:
      paths:
      - path: {{ .Values.calculate.ingress.path }}
        pathType: {{ .Values.calculate.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.calculate.service.name }}
            port:
              number: 80
{{- end }}
