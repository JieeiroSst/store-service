{{- if .Values.bankingService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.bankingService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.bankingService.name }}-deployment
  minReplicas: {{ .Values.bankingService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bankingService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bankingService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
