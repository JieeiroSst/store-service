apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.bankingService.name }}-deployment
spec:
{{- if not .Values.bankingService.autoscaling.enabled }}
  replicas: {{ .Values.bankingService.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.bankingService.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.bankingService.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.bankingService.name }}-deployment
        image: {{ .Values.bankingService.image.name }}:{{ .Values.bankingService.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.bankingService.image.port }}
        env:
          - name: PORT
            value: "{{ .Values.bankingService.image.port }}"
        resources:
          {{- toYaml .Values.bankingService.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: banking-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.bankingService.service.name }}
spec:
  type: {{ .Values.bankingService.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.bankingService.image.port }}
  selector:
    app: {{ .Values.bankingService.name }}-deployment
