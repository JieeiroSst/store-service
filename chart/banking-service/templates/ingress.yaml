{{- if .Values.bankingService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.bankingService.name }}-ingress
  {{- with .Values.bankingService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.bankingService.ingress.className }}
  rules:
  - host: {{ .Values.bankingService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.bankingService.ingress.path }}
        pathType: {{ .Values.bankingService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.bankingService.service.name }}
            port:
              number: 80
{{- end }}
