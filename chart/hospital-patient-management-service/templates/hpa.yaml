{{- if .Values.hospitalPatientManagementService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.hospitalPatientManagementService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.hospitalPatientManagementService.name }}-deployment
  minReplicas: {{ .Values.hospitalPatientManagementService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.hospitalPatientManagementService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hospitalPatientManagementService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
