apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deliveryService.name }}-deployment
spec:
{{- if not .Values.deliveryService.autoscaling.enabled }}
  replicas: {{ .Values.deliveryService.replicas }}
{{- end }}
  selector:
    matchLabels:
      app: {{ .Values.deliveryService.name }}-deployment
  template:
    metadata:
      labels:
        app: {{ .Values.deliveryService.name }}-deployment
    spec:
      containers:
      - name: {{ .Values.deliveryService.name }}-deployment
        image: {{ .Values.deliveryService.image.name }}:{{ .Values.deliveryService.image.tag }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.deliveryService.image.port }}
        resources:
          {{- toYaml .Values.deliveryService.resources | nindent 10 }}
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: delivery-service.env
      volumes:
      - name: env-file
        secret:
          secretName: service-env-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deliveryService.service.name }}
spec:
  type: {{ .Values.deliveryService.service.type }}
  ports:
  - port: 80
    targetPort: {{ .Values.deliveryService.image.port }}
  selector:
    app: {{ .Values.deliveryService.name }}-deployment
