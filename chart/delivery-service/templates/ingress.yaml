{{- if .Values.deliveryService.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.deliveryService.name }}-ingress
  {{- with .Values.deliveryService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.deliveryService.ingress.className }}
  rules:
  - host: {{ .Values.deliveryService.ingress.host }}
    http:
      paths:
      - path: {{ .Values.deliveryService.ingress.path }}
        pathType: {{ .Values.deliveryService.ingress.pathType }}
        backend:
          service:
            name: {{ .Values.deliveryService.service.name }}
            port:
              number: 80
{{- end }}
