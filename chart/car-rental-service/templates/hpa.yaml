{{- if .Values.carRentalService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.carRentalService.name }}-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.carRentalService.name }}-deployment
  minReplicas: {{ .Values.carRentalService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.carRentalService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.carRentalService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
