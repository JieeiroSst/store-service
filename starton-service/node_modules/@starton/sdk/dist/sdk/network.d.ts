import * as operations from "../sdk/models/operations";
import * as shared from "../sdk/models/shared";
import { Rpc } from "./rpc";
import { SDKConfiguration } from "./sdk";
import { AxiosRequestConfig } from "axios";
/**
 * Get all available network, or add your custom one (enterprise only)
 */
export declare class Network {
    rpc: Rpc;
    private sdkConfiguration;
    constructor(sdkConfig: SDKConfiguration);
    /**
     * Create a new network (Enterprise)
     *
     * @remarks
     * Adds a new network to the project, available only for Enterprise plans.
     */
    create(req: shared.CreateNetworkDto, config?: AxiosRequestConfig): Promise<operations.CreateNetworkResponse>;
    /**
     * Delete a network (Enterprise)
     *
     * @remarks
     * Removes a network from the project, available only for Enterprise plans.
     */
    delete(req: operations.DeleteNetworkRequest, config?: AxiosRequestConfig): Promise<operations.DeleteNetworkResponse>;
    /**
     * Retrieve the list of supported networks
     *
     * @remarks
     * Fetches a paginated list of networks available in the current project.
     */
    getAll(req: operations.GetAllNetworkRequest, config?: AxiosRequestConfig): Promise<operations.GetAllNetworkResponse>;
    /**
     * Fetch a specific network
     *
     * @remarks
     * Retrieves detailed information about a specific network based on its name.
     */
    getOne(req: operations.GetOneNetworkRequest, config?: AxiosRequestConfig): Promise<operations.GetOneNetworkResponse>;
    /**
     * Update specific network details (Enterprise)
     *
     * @remarks
     * Modifies the details of a specific network based on its unique name. This feature is only available to Entreprise plans.
     */
    update(req: operations.UpdateNetworkRequest, config?: AxiosRequestConfig): Promise<operations.UpdateNetworkResponse>;
}
